server:
  port: 8085
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/jhoncone/springcloud
          search-paths: config-data
  application:
    name: config-service
